% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all.R
\name{lmsqreg.fit}
\alias{lmsqreg.fit}
\title{Fit LMS quantile regression}
\usage{
lmsqreg.fit(
  YY,
  TT,
  edf = c(3, 5, 3),
  targlen = 50,
  targetx = seq(min(TT), max(TT), length = targlen),
  pvec = c(0.05, 0.1, 0.25, 0.5, 0.75, 0.9, 0.95),
  maxit = 15,
  tol = 0.01,
  verb = FALSE,
  lam.fixed = NULL,
  mu.fixed = NULL,
  sig.fixed = NULL,
  xcuts = quantile(TT, c(0.2, 0.4, 0.6, 0.8)),
  sig.init = mad(YY)/median(YY),
  lam.init = NULL
)
}
\arguments{
\item{YY}{numeric response vector (must be positive; shifted automatically if
any values are below 1)}

\item{TT}{numeric predictor vector}

\item{edf}{length-3 numeric vector of equivalent degrees of freedom for the
lambda, mu, and sigma smoothing splines}

\item{targlen}{integer; number of points in the target x grid}

\item{targetx}{numeric vector of x values at which quantiles are evaluated}

\item{pvec}{numeric vector of probabilities for quantile output}

\item{maxit}{integer; maximum number of Fisher-scoring iterations}

\item{tol}{numeric; convergence tolerance (max absolute parameter change)}

\item{verb}{logical; if \code{TRUE}, print parameter-change ranges each iteration}

\item{lam.fixed}{numeric scalar; if not \code{NULL}, hold lambda fixed at this
value throughout}

\item{mu.fixed}{numeric scalar; if not \code{NULL}, hold mu fixed at this value}

\item{sig.fixed}{numeric scalar; if not \code{NULL}, hold sigma fixed at this
value}

\item{xcuts}{numeric vector of x-quantile cut points used to define
intervals for goodness-of-fit tests}

\item{sig.init}{initial sigma value(s); length 1 or \code{length(YY)}}

\item{lam.init}{initial lambda value(s); length 1 or \code{length(YY)}}
}
\value{
An object of class \code{"lmsqreg.fit"}, a list with three components:
\describe{
\item{lms.ans}{list of fitted LMS curves and convergence diagnostics}
\item{qsys}{quantile system, class \code{"qsys.out"}, with \code{outmat},
\code{targetx}, \code{pcts}, and \code{edf}}
\item{validout}{in-sample quantile coverage from \code{validate()}}
}
}
\description{
Fits the LMS (Lambda-Mu-Sigma) quantile regression model of Cole and Green
(1992) using penalized likelihood with cubic smoothing splines.
}
\examples{
set.seed(123)
x <- runif(300, 10, 20)
y <- 8 + 2 * sin(x) + rnorm(300, 0, x / 11)
fit <- lmsqreg.fit(y, x, maxit = 25)
print(fit)

}
\references{
Cole, T.J. and Green, P.J. (1992). Smoothing reference centile
curves: The LMS method and penalized likelihood. \emph{Statistics in Medicine},
\strong{11}, 1305–1319.
}
