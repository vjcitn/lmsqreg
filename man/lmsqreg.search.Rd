% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all.R
\name{lmsqreg.search}
\alias{lmsqreg.search}
\title{Grid search over EDF parameters}
\usage{
lmsqreg.search(
  y,
  x,
  startedf = c(3, 5, 3),
  boundedf = c(1, 2, 1),
  search.seq = c(1, 3, 2),
  ...
)
}
\arguments{
\item{y}{numeric response vector}

\item{x}{numeric predictor vector}

\item{startedf}{length-3 numeric vector; starting EDF values}

\item{boundedf}{length-3 numeric vector; minimum EDF values (search stops
when an EDF component reaches its bound)}

\item{search.seq}{integer vector; order in which EDF components are searched
(e.g. \code{c(1,3,2)} searches lambda, then sigma, then mu)}

\item{...}{further arguments passed to \code{\link[=lmsqreg.fit]{lmsqreg.fit()}}}
}
\value{
A list with components:
\describe{
\item{fit}{the best-fitting \code{lmsqreg.fit} object found}
\item{pl}{its penalized log-likelihood}
\item{edf}{the optimal EDF triple}
}
}
\description{
Optimizes the equivalent degrees of freedom (EDF) triple for an LMS fit by
iteratively reducing each EDF component while the penalized log-likelihood
improves by more than 2 units.
}
