% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all.R
\name{zscores}
\alias{zscores}
\title{Compute LMS z-scores for new data}
\usage{
zscores(y, x, obj)
}
\arguments{
\item{y}{numeric vector of response values}

\item{x}{numeric vector of predictor values (same length as \code{y})}

\item{obj}{an object of class \code{"lmsqreg.fit"}}
}
\value{
numeric vector of z-scores of the same length as \code{y}
}
\description{
Transforms arbitrary \verb{(y, x)} pairs to standard-normal z-scores using the
LMS functions from a fitted \code{lmsqreg.fit} object. Linear interpolation is
used within the fitted x range; constant extrapolation is applied outside
it (with a warning).
}
\examples{
set.seed(123)
xf <- runif(200, 10, 20)
yf <- 8 + 2 * sin(xf) + rnorm(200, 0, xf / 11)
fit <- lmsqreg.fit(yf, xf)
xnew <- runif(50, 10, 20)
ynew <- 8 + 2 * sin(xnew) + rnorm(50, 0, xnew / 11)
z <- zscores(ynew, xnew, fit)
cat("z-score mean:", round(mean(z), 3), "\n")
}
